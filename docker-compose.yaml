services:
  backend:
    build: .
    container_name: artgallery-backend
    ports:
      - "127.0.0.1:8001:8000"
    env_file:
      - .env
    networks:
      - nginx_default

networks:
  nginx_default:
    external: true